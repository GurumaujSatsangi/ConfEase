<!DOCTYPE html>
<html lang="en">
 <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>DEI CMT</title>
    <link rel="shortcut icon" href="/dei_logo.png" type="image/x-icon">
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>
    
    <%-include('../partials/dashboard/header') %>

      <div class="container">
        
        <h4>Manage Oral Presentation Sessions</h4>
        <h6>Conference: <%= conference.title %></h6>
        <hr />
        
          <% if (tracks && tracks.length > 0) { %> <%
          tracks.forEach(function(track, idx) { // Find the count for this track
          var trackCount = 0; if (count && count.length > 0) { var found =
          count.find(function(tc) { return tc.track_id === track.track_id; }); if
          (found) trackCount = found.count; } %>
          <div class="card mb-4">
            <div class="card-body p-4">
              <div class="d-flex justify-content-between align-items-start mb-3">
                <div class="flex-grow-1">
                  <h6 class="card-title mb-0"><%= track.track_name %></h6>
                </div>
                <span class="badge bg-info text-dark ms-2">
                  <i class="bi bi-file-earmark me-1"></i><%= trackCount %> Submissions
                </span>
              </div>
              
              <div class="table-responsive">
                <table class="table table-sm table-hover mb-0">
                  <thead class="table-light">
                    <tr>
                      <th scope="col">Reviewers</th>
                      <th scope="col">Session Date</th>
                      <th scope="col">Start Time</th>
                      <th scope="col">End Time</th>
                      <th scope="col">Session Chair</th>
                    </tr>
                  </thead>
                  <tbody>
                    <tr>
                      <td><% if (Array.isArray(track.track_reviewers)) { %><%= track.track_reviewers.join(", ") %><% } else { %><%= track.track_reviewers %><% } %></td>
                      <td><%= track.presentation_date %></td>
                      <td><%= track.presentation_start_time %></td>
                      <td><%= track.presentation_end_time %></td>
                      <td><%= track.panelists %></td>
                    </tr>
                  </tbody>
                </table>
              </div>

              <!-- Leaderboard Accordion -->
              <div class="accordion mt-4" id="accordion-<%= idx %>">
                <div class="accordion-item">
                  <h2 class="accordion-header" id="heading-<%= idx %>">
                    <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapse-<%= idx %>" aria-expanded="false" aria-controls="collapse-<%= idx %>">
                      <i class="bi bi-trophy me-2"></i>View Leaderboard
                    </button>
                  </h2>
                  <div id="collapse-<%= idx %>" class="accordion-collapse collapse" aria-labelledby="heading-<%= idx %>" data-bs-parent="#accordion-<%= idx %>">
                    <div class="accordion-body p-0">
                      <% if (track.leaderboard && track.leaderboard.length > 0) { %>
                        <div class="table-responsive">
                          <table class="table table-striped table-sm table-hover mb-0 leaderboard-table">
                            <thead class="table-light">
                              <tr>
                                <th style="min-width: 60px;">Rank</th>
                                <th style="min-width: 150px;">Paper Title</th>
                                <th style="min-width: 120px;">Primary Author</th>
                                <th style="min-width: 120px;">Co-Authors</th>
                                <th style="min-width: 90px;">Reviewer</th>
                                <th style="min-width: 90px;">Panelist</th>
                                <th style="min-width: 90px;">Average</th>
                              </tr>
                            </thead>
                            <tbody>
                              <% track.leaderboard.forEach(function(submission) { %>
                                <tr>
                                  <td style="white-space: nowrap;">
                                    <% if (submission.rank) { %>
                                      <% if (submission.rank === 1) { %>
                                         <i class="bi bi-1-circle-fill"></i>
                                      <% } else if (submission.rank === 2) { %>
                                         <i class="bi bi-2-circle-fill"></i>
                                      <% } else if (submission.rank === 3) { %>
                                         <i class="bi bi-3-circle-fill"></i>
                                      <% } else { %>
                                        #<%= submission.rank %>
                                      <% } %>
                                    <% } else { %>
                                      <span class="text-muted">N/A</span>
                                    <% } %>
                                  </td>
                                  <td>
                                    <div class="fw-bold"><%= submission.title %></div>
                                    <small class="text-muted">Status: <%= submission.submission_status %></small>
                                  </td>
                                  <td><%= submission.primary_author_formatted %></td>
                                  <td><%= submission.co_authors_formatted %></td>
                                  <td style="text-align: center;">
                                    <% if (submission.reviewerScore !== null) { %>
                                      <span class="badge bg-primary"><%= submission.reviewerScore %>/5</span>
                                    <% } else { %>
                                      <span class="badge bg-secondary">N/A</span>
                                    <% } %>
                                  </td>
                                  <td style="text-align: center;">
                                    <% if (submission.panelistScore !== null) { %>
                                      <span class="badge bg-success"><%= submission.panelistScore %>/5</span>
                                    <% } else { %>
                                      <span class="badge bg-secondary">N/A</span>
                                    <% } %>
                                  </td>
                                  <td style="text-align: center;">
                                    <% if (submission.averageScore !== null) { %>
                                      <span class="badge bg-<%= submission.averageScore >= 4 ? 'success' : submission.averageScore >= 3 ? 'warning' : 'danger' %> fs-6">
                                        <%= submission.averageScore %>/5
                                      </span>
                                    <% } else { %>
                                      <span class="badge bg-secondary">N/A</span>
                                    <% } %>
                                  </td>
                                </tr>
                              <% }); %>
                            </tbody>
                          </table>
                        </div>
                      <% } else { %>
                        <div class="text-center text-muted p-4">
                          <i class="bi bi-inbox display-4"></i>
                          <p class="mt-2"><b>No submissions have been marked as "Completed"!</b></p>
                        </div>
                      <% } %>
                    </div>
                  </div>
                </div>
              </div>

            
            
            </div>
          </div>

    <!-- Final Camera Ready Papers -->
<div class="mt-4">
  <h5>Final Camera Ready Papers (Accepted for Oral Presentation)</h5>
  <hr />

  <% if (track.finalCameraReadyPapers && track.finalCameraReadyPapers.length > 0) { %>
    <div class="table-responsive">
      <table class="table table-sm table-hover">
        <thead class="table-light">
          <tr>
            <th>Title</th>
            <th>Primary Author</th>
            <th>Co-Authors</th>
            <th>Mean Score (Peer Review)</th>
            <th>View Paper</th>
          </tr>
        </thead>
        <tbody>
          <% track.finalCameraReadyPapers.forEach(function(paper) { %>
            <tr>
              <td class="fw-bold"><%= paper.title %></td>
              <td><%= paper.primary_author %></td>
              <td>
                <% if (paper.co_authors && paper.co_authors.length > 0) { %>
                  <%= paper.co_authors.join(", ") %>
                <% } else { %>
                  <span class="text-muted">—</span>
                <% } %>
              </td>
              <td class="text-center">
                <% if (paper.mean_score) { %>
                  <span class="badge bg-primary"><%= paper.mean_score %>/5</span>
                <% } else { %>
                  <span class="badge bg-secondary">N/A</span>
                <% } %>
              </td>
              <td>
                <% if (paper.paper_url) { %>
                  <a href="<%= paper.paper_url %>" target="_blank" class="btn btn-sm btn-outline-primary">
                    View
                  </a>
                <% } else { %>
                  <span class="text-muted">Not uploaded</span>
                <% } %>
              </td>
            </tr>
          <% }); %>
        </tbody>
      </table>
    </div>
  <% } else { %>
<h6>      <i class="bi bi-emoji-frown-fill me-1"></i>
      No Final Camera Ready Papers have been submitted!
</h6>
  <% } %>
</div>


          <% }); %> <% } else { %>
          <h6><i class="bi bi-emoji-frown-fill"></i> No tracks have been added for this conference, please go back to the dashboard and click on 'Manage Tracks' to add tracks!</h6>
          <% } %>
          <br />
          
       

       
      </div>
    <div
      class="modal fade"
      id="loadingModal"
      tabindex="-1"
      aria-hidden="true"
      data-bs-backdrop="static"
      data-bs-keyboard="false"
    >
      <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content bg-transparent border-0 shadow-none">
          <div
            class="modal-body d-flex justify-content-center align-items-center"
          >
            <div
              class="spinner-border text-primary"
              style="width: 3rem; height: 3rem"
              role="status"
            >
              <span class="visually-hidden"
                >Please wait...submitting data.</span
              >
            </div>
          </div>
        </div>
      </div>
    </div>

  </div>
  <script src="/script.js"></script>

 <%-include('../partials/footer') %>
  </body>
</html>
